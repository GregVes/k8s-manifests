kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.postgres.pvc }}
  labels:
    {{- include "matrix.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.postgres.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgres.storageSize }}